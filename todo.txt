--gtest_filter=-Font*:Face*
--gtest_filter=Library*:Family*

Makefile

- Python build script

Contours

- Separate class for converting splines to contours
	- Tests
	
- SetPrecisionHint(amount);
- SetPrecisionType((eFixed | eDynamic));

ShapePath
- Move(position)
- LineTo(position)
- ConicTo(position, control);
- CubicTo(position, controlA, controlB);
- Visit(visitor, precisionType, precisionHint)

GraphicsSettings
- winding
- precision

FilledSettings
- winding
- precision
- type
	eFast
	eSmooth
- thickness

Canvas
- Clear()
- Move(position)
- LineTo(position)
- ConicTo(position, control);
- CubicTo(position, controlA, controlB);
- Finish()
- MeshBuilder* BuildOutline(settings)
- MeshBuilder* BuildFilled(settings)

TextLayout
- 

MeshCache
- FindMesh(unsigned int)
- AddMesh(unsigned int, Mesh*)

FontRenderOutline(FontFace*)
- FindMesh(int)
- LoadGlyphs(vector<int>)
- DrawText(string, position, color)

struct FontOptions
- size
- weight
- italic

library->AddLoader(loader);
library->LoadFont("Roboto.fnt");
library->FindFamily("Roboto")->FindFont(weight | italic)->FindFace(size)

int exlInitialize()
int exlLoadFontFromFile(FILE* handle)
int exlLoadFontFromBuffer(const char* buffer, size_t size)
int exlFindFamily(int* handle, const char* name)
int exlBindFamily(int handle)
int exlSetParameterI(int parameter, int value)
int exlCreateFont(int count, int* handle)


int family_handle = 1;
exlBindFamily(family_handle);

exlSetFaceParameterI(EXL_PARAMETER_WEIGHT, EXL_WEIGHT_BOLD);
exlSetFaceParameterI(EXL_PARAMETER_ITALIC, EXL_TRUE);
exlSetFaceParameterF(EXL_PARAMETER_SIZE, 20.0f);

int font_handle;
exlCreateFont(1, &font_handle);