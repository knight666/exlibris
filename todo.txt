Makefile

- Python build script

Contours

- Separate class for converting splines to contours
	- Tests
	
- SetPrecisionHint(amount);
- SetPrecisionType((eFixed | eDynamic));

ShapePath
- Move(position)
- LineTo(position)
- ConicTo(position, control);
- CubicTo(position, controlA, controlB);
- Visit(visitor, precisionType, precisionHint)

GraphicsSettings
- winding
- precision

FilledSettings
- winding
- precision
- type
	eFast
	eSmooth
- thickness

IMeshBuilder
- SetVertexCount(count)
- AddVertexPosition(position)
- AddTriangle(indexA, indexB, indexC)

GraphicsContext
- Clear()
- Move(position)
- LineTo(position)
- ConicTo(position, control);
- CubicTo(position, controlA, controlB);
- Finish()
- BuildMeshFilled(IMeshBuilder, settings)
- BuildMeshOutline(IMeshBuilder, settings)