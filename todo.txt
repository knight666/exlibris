Makefile

- Python build script

Contours

- Separate class for converting splines to contours
	- Tests
	
- SetPrecisionHint(amount);
- SetPrecisionType((eFixed | eDynamic));

ShapePath
- Move(position)
- LineTo(position)
- ConicTo(position, control);
- CubicTo(position, controlA, controlB);
- Visit(visitor, precisionType, precisionHint)

GraphicsSettings
- winding
- precision

FilledSettings
- winding
- precision
- type
	eFast
	eSmooth
- thickness

IMeshBuilder
- SetVertexCount(count)
- AddVertexPosition(position)
- AddTriangle(indexA, indexB, indexC)

Cursor
- command_index
- position_index
- step
- delta

CurvePathCursor
- ReadPosition()
- IsNextShape()

CurvePath
- ReadPosition()
- GetCurrentShapeIndex()
- IsEndOfPath()

Canvas
- Clear()
- Move(position)
- LineTo(position)
- ConicTo(position, control);
- CubicTo(position, controlA, controlB);
- Finish()
- MeshBuilder* BuildOutline(settings)
- MeshBuilder* BuildFilled(settings)

IMeshVisitor
- GetOrientation()
- VisitBuilderVertexCount(count)
- VisitBuilderAddTriangle(a, b, c)

MeshBuilder
- AddTriangle(a, b, c)
- AddQuad(ul, ur, ll, lr)
- Accept(visitor)


IPositionReader
- CursorStart()
- CursorReadPosition()
- CursorIsNextShape()
- CursorIsValid()

